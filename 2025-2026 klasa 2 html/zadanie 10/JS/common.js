function randomNumber() {
  return Math.floor(Math.random() * 100);
}

function sortNumbers(Numbers) {
  if (a < b) {
    return -1;
  } else if (a > b) {
    return 1;
  } else if (a === b) {
    return 0;
  }
}
